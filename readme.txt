1)type npm install in your current directory to install all the dependencies. if you dont have node installed please install node. 
2) type npm start to start the server in the command prompt in the directory.  
